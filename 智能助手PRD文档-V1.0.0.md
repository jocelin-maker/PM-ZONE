# 智能助手 PRD 文档

---

| 字段 | 内容 |
|------|------|
| 文档名称 | 智能助手产品需求文档 |
| 版本号 | V-1.0.0 |
| 版本内容 | 新增智能助手板块 |
| 负责人 | XXX |
| 创建日期 | 2026-06-09 |
| 最后更新 | 2026-06-11 |
| 文档状态 | 待评审 |

---

## 修订历史

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|---------|
| V-1.0.0 | 2026-06-09 | XXX | 初稿创建，覆盖登录、首页、角色选择、对话工作区、历史记录、设置、后台管理七大模块 |
| V-1.0.1 | 2026-06-11 | XXX | 补充用户画像、成功指标、信息架构、非功能性需求、数据模型、API规范、测试要求、术语表等完整章节 |

---

## 目录

1. [文档说明](#1-文档说明)
2. [产品背景与目标](#2-产品背景与目标)
3. [用户画像](#3-用户画像)
4. [成功指标（KPI）](#4-成功指标kpi)
5. [信息架构与页面流转](#5-信息架构与页面流转)
6. [功能模块详细设计](#6-功能模块详细设计)
   - [6.1 登录模块](#61-登录模块)
   - [6.2 首页模块](#62-首页模块)
   - [6.3 角色选择模块](#63-角色选择模块)
   - [6.4 对话工作区](#64-对话工作区引导页与对话页融合)
   - [6.5 历史记录管理（侧边栏）](#65-历史记录管理侧边栏)
   - [6.6 设置与偏好](#66-设置与偏好)
   - [6.7 博美智研后台](#67-博美智研后台)
7. [异常与边界规则](#7-异常与边界规则)
8. [非功能性需求](#8-非功能性需求)
9. [数据模型](#9-数据模型)
10. [接口需求说明](#10-接口需求说明)
11. [埋点与数据分析](#11-埋点与数据分析)
12. [测试要求](#12-测试要求)
13. [发布与灰度计划](#13-发布与灰度计划)
14. [待确认问题（Open Questions）](#14-待确认问题open-questions)
15. [术语表](#15-术语表)

---

## 1 文档说明

### 1.1 目的

本文档为"智能助手"功能模块的产品需求文档（PRD），面向研发、设计、测试、运营等协作团队，描述功能范围、交互逻辑、数据规则及质量要求，作为多团队协同的统一基准。

### 1.2 范围

本期（V-1.0.0）覆盖以下新增功能：

- 微信授权登录
- 智能助手首页（热门推荐）
- 角色选择模块
- 对话工作区（引导页 + 对话页）
- 历史记录侧边栏
- 用户设置中"核心角色"配置
- 博美智研后台"核心角色"字段管理

### 1.3 阅读对象

| 角色 | 关注重点 |
|------|---------|
| 产品经理 | 全文 |
| UI/UX 设计师 | 第 5、6 章（页面流转 + 详细设计）|
| 前端工程师 | 第 6、7、8、10 章（功能细节 + 接口 + 非功能）|
| 后端工程师 | 第 9、10 章（数据模型 + 接口）|
| 测试工程师 | 第 7、12 章（边界规则 + 测试要求）|
| 运营 | 第 4、11 章（KPI + 埋点）|

---

## 2 产品背景与目标

### 2.1 背景

美妆行业高速迭代，配方研发、成分合规、产品规划等专业场景对高质量知识检索和智能问答的需求日益强烈。现有产品缺乏垂直领域的智能对话能力，用户只能通过人工咨询或分散的网络资料获取信息，效率低、专业性不足。

基于此，平台引入大语言模型（LLM）能力，叠加百万级成分数据库与法规知识库，面向美妆产业链的核心从业角色推出"智能助手"功能，打造专业、可信、高效的垂直 AI 问答产品。

### 2.2 产品定位

> **"美妆产业专属的 AI 智能顾问，让每位从业者都能随时获得专家级解答。"**

### 2.3 版本目标

| 目标类型 | 具体描述 |
|---------|---------|
| 功能目标 | 完成智能助手核心闭环：登录 → 角色设定 → 提问 → 获取专业回答 → 历史管理 |
| 体验目标 | 流式输出渲染流畅，首 Token 响应 ≤ 3 秒，用户无明显等待感 |
| 质量目标 | 上线首月敏感词拦截准确率 ≥ 99%，用户负向反馈率 < 8% |
| 运营目标 | 上线 30 天内，日活使用智能助手的用户占比 DAU ≥ 20% |

### 2.4 项目干系人

| 角色 | 姓名 | 职责 |
|------|------|------|
| 产品负责人 | XXX | 需求决策、版本把控 |
| 设计负责人 | TBD | UI/UX 设计交付 |
| 前端负责人 | TBD | 客户端实现 |
| 后端负责人 | TBD | 服务端 + AI 接口集成 |
| 测试负责人 | TBD | 质量保障 |
| 算法/AI 负责人 | TBD | 大模型接入、Prompt 工程 |
| 运营负责人 | TBD | 内容运营、数据监控 |

---

## 3 用户画像

### 3.1 核心用户群体

本产品面向美妆产业链的 B 端从业者，非 C 端消费者。

---

**Persona 1 — 美妆产品经理（主力用户）**

| 维度 | 描述 |
|------|------|
| 年龄 | 25–38 岁 |
| 工作内容 | 负责产品规划、原料选型、配方成分审查、SKU 生命周期管理 |
| 核心痛点 | 成分功效信息分散、合规审查耗时、缺乏快速验证原料可行性的工具 |
| 使用场景 | 日常查询成分搭配禁忌、快速了解竞品成分趋势、验证配方思路 |
| 期望收益 | 节省信息检索时间 50%+，降低沟通成本 |

---

**Persona 2 — 配方研发工程师**

| 维度 | 描述 |
|------|------|
| 年龄 | 28–45 岁 |
| 工作内容 | 配方创新、工艺研发、原料替代方案研究、稳定性测试 |
| 核心痛点 | 专业资料检索门槛高、复杂工艺问题需要多方查阅、新法规解读不及时 |
| 使用场景 | 配方灵感验证、原料文献快速检索、工艺参数咨询 |
| 期望收益 | 研发效率提升，降低试错成本 |

---

**Persona 3 — 法律合规人员**

| 维度 | 描述 |
|------|------|
| 年龄 | 30–50 岁 |
| 工作内容 | 产品注册备案、全球法规合规审核、标签审核、禁限用物质核查 |
| 核心痛点 | 各国法规更新频繁、合规查阅耗时、无法快速定位相关法条 |
| 使用场景 | 快速查询某成分在特定国家的使用限制、获取法规原文引用 |
| 期望收益 | 合规审核效率提升，降低违规风险 |

---

**Persona 4 — 细分领域专家**

| 维度 | 描述 |
|------|------|
| 年龄 | 30–55 岁 |
| 工作内容 | 毒理、生物、化学合成等专业领域支持，为多项目提供技术咨询 |
| 核心痛点 | 跨领域知识整合难度大，需要快速响应多项目的技术问题 |
| 使用场景 | 深度专业问题研究、技术报告辅助生成 |
| 期望收益 | 知识整合效率提升，支持更多项目并行 |

---

## 4 成功指标（KPI）

### 4.1 核心指标

| 指标 | 定义 | 目标值（上线 30 天） | 数据来源 |
|------|------|-------------------|---------|
| 功能渗透率 | 使用智能助手的 DAU / 总 DAU | ≥ 20% | 埋点 |
| 次日留存率 | 使用智能助手后次日回访用户占比 | ≥ 40% | 埋点 |
| 首 Token 响应时长 | 从发送到首个字符流式返回的时长 P90 | ≤ 3 秒 | 服务端监控 |
| 完整回答时长 | 平均每次回答完整输出时长 P90 | ≤ 30 秒 | 服务端监控 |
| 正向反馈率 | 点赞 / (点赞 + 点踩) | ≥ 80% | 埋点 |
| 负向反馈率 | 点踩会话占总会话比例 | < 8% | 埋点 |
| 敏感词拦截准确率 | 正确拦截数 / 应拦截总数 | ≥ 99% | 后台监控 |
| 角色设置完成率 | 完成角色选择 / 触发角色选择弹窗 | ≥ 60% | 埋点 |
| 会话深度 | 单次会话平均轮数 | ≥ 3 轮 | 埋点 |
| 接口错误率 | AI 接口调用失败次数 / 总调用次数 | < 1% | 服务端监控 |

### 4.2 次要指标（观察项）

| 指标 | 说明 |
|------|------|
| 模式偏好分布 | 快速模式 vs 专家模式使用比例 |
| 语调偏好分布 | 亲和语调 vs 专业语调使用比例 |
| 重新生成频率 | 每会话平均重新生成次数 |
| 猜你想问点击率 | 追问胶囊点击量 / 展示量 |
| 微信登录占比 | 微信登录用户 / 新增总用户 |

---

## 5 信息架构与页面流转

### 5.1 整体信息架构

```
智能助手
├── 登录模块
│   ├── 账号密码登录
│   └── 微信授权登录
│       ├── 已绑定手机号 → 直接登录
│       └── 未绑定手机号 → 手机号绑定页
├── 首页（智能助手入口页）
│   ├── 热门话题推荐区
│   └── 智能输入框
├── 角色选择弹窗（首次登录强制）
├── 对话工作区
│   ├── 空状态（引导页）
│   │   ├── 欢迎卡片
│   │   ├── 推荐问题
│   │   └── 全局参数配置
│   └── 对话状态
│       ├── 用户提问区
│       ├── 模型思考过程（可折叠）
│       ├── 系统回答区
│       ├── 结果操作栏（点赞/踩/复制/重新生成）
│       ├── 猜你想问（追问胶囊）
│       └── 底部输入区
├── 历史记录侧边栏
│   ├── 新建对话按钮
│   └── 历史会话列表
│       └── 会话操作（改名/删除）
└── 设置与偏好
    └── 核心角色配置
```

### 5.2 页面流转图

```
┌──────────────────────────────────────────────────────────────────┐
│                          APP 启动                                 │
└────────────────────────┬─────────────────────────────────────────┘
                         │
              ┌──────────▼──────────┐
              │    未登录态检测      │
              └──────────┬──────────┘
                         │
        ┌────────────────┼────────────────┐
        │ 点击热门话题/输入框              │ 直接访问（已登录）
        ▼                                 ▼
┌──────────────┐               ┌──────────────────┐
│  登录页      │               │   智能助手首页    │
│  账号密码    │               └────────┬─────────┘
│  微信授权    │                        │
└──────┬───────┘            ┌───────────┼───────────┐
       │                    │           │           │
       ▼                    │     角色未设置    角色已设置
┌─────────────┐             │           ▼           ▼
│ 已绑定手机  │─────登录成功─▶│    ┌──────────┐  ┌──────────┐
│ 未绑定手机  │─────绑定后──▶│    │ 角色选择  │  │  对话    │
└─────────────┘             │    │  弹窗     │  │  工作区  │
                            │    └────┬─────┘  └──────────┘
                            │         │
                            │    ┌────▼─────┐
                            │    │确认/跳过  │
                            │    └────┬─────┘
                            │         ▼
                            │    ┌──────────┐
                            └───▶│ 对话工作区│
                                 │ （引导页）│
                                 └────┬─────┘
                                      │ 提问
                                      ▼
                                 ┌──────────┐
                                 │ 对话状态  │
                                 └──────────┘
```

### 5.3 对话工作区内部状态机

```
引导页（空状态）
    │
    │ 用户输入并发送 / 点击推荐问题 / 点击猜你想问
    ▼
对话进行中（流式输出）
    │
    ├─ 点击"停止生成" ──────────────▶ 对话已中断态（展示已输出内容）
    │
    └─ 输出完成 ─────────────────────▶ 对话完成态
                                            │
                                ┌───────────┼───────────┐
                                │           │           │
                           点击重生    点击猜你想问   输入新问题
                                │           │           │
                                └───────────┴───────────┘
                                            │
                                      对话进行中
```

### 5.4 页面清单

| 页面 ID | 页面名称 | 所属模块 | 页面类型 |
|---------|---------|---------|---------|
| P-01 | 登录页 | 登录模块 | 全页面 |
| P-02 | 手机号绑定页 | 登录模块 | 全页面 |
| P-03 | 智能助手首页 | 首页模块 | 全页面 |
| P-04 | 角色选择弹窗 | 角色选择模块 | 底部弹出层 |
| P-05 | 对话工作区（引导页） | 对话工作区 | 全页面 |
| P-06 | 对话工作区（对话页） | 对话工作区 | 全页面 |
| P-07 | 点踩反馈弹窗 | 对话工作区 | 弹窗 |
| P-08 | 历史记录侧边栏 | 历史记录管理 | 侧边抽屉 |
| P-09 | 会话重命名弹窗 | 历史记录管理 | 弹窗 |
| P-10 | 设置-核心角色配置页 | 设置与偏好 | 二级页面/底部弹窗 |

---

## 6 功能模块详细设计

### 6.1 登录模块

#### 6.1.1 功能入口

| 触发场景 | 触发条件 | 跳转目标 |
|---------|---------|---------|
| 点击首页热门话题中任意一个问题 | 用户未登录 | P-01 登录页 |
| 点击首页输入框区域 | 用户未登录 | P-01 登录页 |
| 其他需要登录态的操作 | 用户未登录 | P-01 登录页 |

> **跳转记忆机制**：登录成功后，需携带用户登录前的操作意图（如点击的热门问题内容）回传，以接续登录前的提问动作。若登录耗时超过 10 分钟，原始意图失效，登录后直接进入首页。

#### 6.1.2 账号密码登录

- 保持原有逻辑不变，不在本次迭代范围内。

#### 6.1.3 微信授权登录（新增）

**流程图：**

```
用户点击"微信登录"
        │
        ▼
   唤起微信内置授权弹窗
        │
   ┌────┴────┐
 授权成功  授权失败/取消
   │           │
   ▼           ▼
  后台验证    返回登录页
  微信账号    Toast:"授权失败，请重试"
   │
   ├── 已绑定手机号 ──▶ 直接完成登录 ──▶ 跳转目标页
   │
   └── 未绑定手机号 ──▶ P-02 手机号绑定页
                              │
                        获取验证码并验证
                              │
                        验证成功 ──▶ 完成注册/登录 ──▶ 跳转目标页
                              │
                        验证失败 ──▶ 提示错误，重试
```

**各状态 UI 规则：**

| 状态 | UI 表现 |
|------|---------|
| 唤起微信授权中 | 按钮 Loading 态，不可重复点击 |
| 授权成功 + 已绑定手机号 | 全屏 Loading，1.5 秒内完成跳转 |
| 授权成功 + 未绑定手机号 | 跳转 P-02 手机号绑定页，顶部提示"微信授权成功，请绑定手机号完成注册" |
| 授权失败或用户取消 | 返回登录页，底部 Toast 弹出"授权失败，请重试"，显示 2 秒自动消失 |
| 网络异常 | 返回登录页，Toast"网络异常，请检查网络后重试" |

**P-02 手机号绑定页：**

| 字段 | 规则 |
|------|------|
| 手机号输入框 | 必填，格式校验：11位纯数字，第1位为1 |
| 获取验证码按钮 | 手机号格式正确时可点击；点击后 60 秒倒计时，期间不可重复点击 |
| 验证码输入框 | 必填，6位数字，超时后需重新获取 |
| 验证码有效期 | 5 分钟 |
| 提交按钮 | 两项均填写且格式正确时可点击 |
| 错误提示 | 验证码错误：输入框下方红色提示"验证码错误，请重新输入"；验证码过期："验证码已过期，请重新获取" |

#### 6.1.4 退出登录

- 路径：**首页 → 更多 → 设置 → 退出登录**
- 退出前弹出确认弹窗，文案："确定要退出登录吗？"，按钮：【取消】【确定退出】
- 确认后：
  1. 清空本地 Token 及用户缓存信息
  2. 清空角色选择缓存（下次登录仍需从服务端读取）
  3. 返回首页（非登录态）
  4. Toast 提示"已成功退出登录"

---

### 6.2 首页模块

#### 6.2.1 页面结构

```
┌─────────────────────────────────────┐
│  顶部导航栏                          │
├─────────────────────────────────────┤
│  主标题区                            │
│  「美妆智能助手」                    │
│  副标题：专业配方分析与法规指导        │
├─────────────────────────────────────┤
│  智能输入框                          │
│  [输入你的问题...]     [发送]        │
├─────────────────────────────────────┤
│  热门话题推荐区                      │
│  ┌────────────┐  ┌────────────┐    │
│  │  推荐问题1  │  │  推荐问题2  │    │
│  └────────────┘  └────────────┘    │
│  ┌────────────┐                    │
│  │  推荐问题3  │                    │
│  └────────────┘                    │
└─────────────────────────────────────┘
```

#### 6.2.2 主标题区

| 元素 | 内容 | 规范 |
|------|------|------|
| 主标题 | 「美妆智能助手」 | 字号大，居中对齐 |
| 副标题 | 基于精准 AI 算法与百万级成分数据库，为你提供专业的配方分析与法规指导 | 字号小，居中，灰色 |

#### 6.2.3 智能输入框

| 属性 | 规则 |
|------|------|
| 占位文案 | "输入你的问题，例如：A 酸与 B 酸可以混合使用吗？" |
| 点击行为（未登录） | 唤起登录流程（P-01） |
| 点击行为（已登录） | 跳转至 P-05/P-06 对话工作区，并聚焦输入框 |
| 发送行为 | 同上；若有内容则携带内容跳转 |

#### 6.2.4 热门话题推荐区

**推荐机制：**

| 优先级 | 场景 | 内容来源 |
|--------|------|---------|
| 1 | 用户已设置核心角色 | 根据角色进行定向推荐，由运营后台配置 |
| 2 | 用户未设置角色 / 接口异常 / 接口超时（>3秒） | 兜底展示 3 个全局通用的美妆百科类问题 |

**兜底问题（固定，运营可配置）：**

1. 「视黄醇和烟酰胺可以同时使用吗？」
2. 「防晒霜的 PA 和 SPF 有什么区别？」
3. 「如何判断一款保湿面霜配方是否稳定？」

**卡片交互：**

| 操作 | 未登录 | 已登录 + 未选角色 | 已登录 + 已选角色 |
|------|--------|-----------------|-----------------|
| 点击任意推荐问题 | 唤起登录 | 唤起角色选择弹窗（P-04），确认后携带该问题跳转对话页 | 携带该问题内容跳转对话页（P-06），并直接开始生成回答 |

#### 6.2.5 刷新推荐

- 用户可点击推荐区右上角「换一换」按钮刷新推荐列表。
- 每日接口拉取最新推荐，本地缓存有效期 1 小时。

---

### 6.3 角色选择模块

#### 6.3.1 触发场景

| 场景 | 触发时机 | 展示方式 |
|------|---------|---------|
| 首次注册成功 | 注册完成后立即弹出 | 全屏底部弹出层（不可通过点击背景关闭） |
| 已登录但未设置角色，点击热门话题/发送问题 | 操作触发时弹出 | 底部弹出层（可通过点击背景关闭，等同于"跳过"） |
| 设置 → 核心角色 | 主动进入 | 二级页面或底部弹出层 |

> **注意**：首次注册强制弹窗仅展示一次。若用户选择"跳过"，则后续不再自动弹出，用户需主动进入设置修改。

#### 6.3.2 页面内容

```
┌─────────────────────────────────────┐
│  请选择你当前的核心角色               │（主标题）
│  选择对应的职业角色以获取定制化体验    │（副标题）
├─────────────────────────────────────┤
│  ○ 美妆产品经理                      │
│    负责产品规划、配方成分及生命周期管理 │
│                                     │
│  ○ 配方研发工程师                    │
│    专注于研发创新，管理复杂的工艺配方  │
│                                     │
│  ○ 法律合规人员                      │
│    审核安全标准，确保产品符合全球法规  │
│                                     │
│  ○ 细分领域专家                      │
│    (毒理/生物/化学合成等)提供深度专业支持│
├─────────────────────────────────────┤
│  [      确认并进入      ]            │
│  跳过（你稍后可以在偏好设置中随时更改）│
└─────────────────────────────────────┘
```

#### 6.3.3 角色枚举值

| 枚举值 | 展示名称 | 描述 | 对应推荐标签 |
|-------|---------|------|------------|
| `PRODUCT_MANAGER` | 美妆产品经理 | 负责产品规划、配方成分及生命周期管理 | 成分分析、产品策划、竞品对标 |
| `FORMULA_ENGINEER` | 配方研发工程师 | 专注于研发创新，管理复杂的工艺配方 | 配方设计、原料替代、工艺优化 |
| `LEGAL_COMPLIANCE` | 法律合规人员 | 审核安全标准，确保产品符合全球法规 | 法规查询、禁限用成分、注册备案 |
| `DOMAIN_EXPERT` | 细分领域专家 | (毒理/生物/化学合成等)提供深度专业支持 | 毒理评估、生物活性、合成路径 |

#### 6.3.4 操作规则

| 操作 | 行为 |
|------|------|
| 选择角色 → 点击「确认并进入」 | 保存角色到服务端；跳转至首页或接续之前的提问动作 |
| 点击「跳过」 | 角色状态记为"未选择"（`null`），不保存；跳转至首页；底部轻提示"你稍后可以在偏好设置中随时更改角色" |
| 未选择角色时点击「确认并进入」 | 按钮置灰或弹出提示"请先选择一个角色" |

---

### 6.4 对话工作区（引导页与对话页融合）

本模块为用户核心工作流，同一页面包含空状态（引导页）与对话状态，状态切换无页面刷新。

#### 6.4.1 空状态（智能助手引导页）

**页面结构：**

```
┌─────────────────────────────────────┐
│  ☰ 历史记录     美妆智能助手   ···   │（顶部导航）
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐    │
│  │  Hi！我是你的美妆智能助理    │    │（欢迎卡片）
│  │  基于精准AI算法与百万级成分  │    │
│  │  数据库，为你提供专业的配方  │    │
│  │  分析与法规指导。            │    │
│  └─────────────────────────────┘    │
│                                     │
│  你可能感兴趣的问题：               │
│  ┌─────────────────────────────┐    │
│  │ 推荐问题 1（按角色定制）      │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 推荐问题 2                  │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 推荐问题 3                  │    │
│  └─────────────────────────────┘    │
│                                     │
├─────────────────────────────────────┤
│  模式：[快速模式 ▼]  语调：[亲和 ▼] │（全局参数配置）
├─────────────────────────────────────┤
│  [输入你的问题...          ] [发送]  │（输入框）
└─────────────────────────────────────┘
```

**欢迎卡片：**

| 元素 | 内容 |
|------|------|
| 标题 | Hi！我是你的美妆智能助理 |
| 正文 | 基于精准 AI 算法与百万级成分数据库，为你提供专业的配方分析与法规指导。 |

**推荐问题：**
- 根据用户当前角色动态拉取，展示 3 个。
- 未设置角色时展示全局兜底问题（同首页逻辑）。
- 点击推荐问题 → 直接发送该问题，进入对话状态。

**全局参数配置：**

| 参数 | 选项 | 默认值 | 说明 |
|------|------|--------|------|
| 模式 | 快速模式 / 专家模式 | 快速模式 | 影响回答深度与排版风格 |
| 语调 | 亲和语调 / 专业语调 | 亲和语调 | 影响回答的表达方式 |

> 参数配置区域放置于底部输入框上方，以下拉选择器形式展示，不遮挡对话内容。

#### 6.4.2 对话状态

##### 6.4.2.1 对话流整体布局

```
┌─────────────────────────────────────┐
│  ☰ 历史记录     美妆智能助手   ···   │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐    │
│  │  [用户头像] 你               │    │ ◀ 用户提问区
│  │  视黄醇和烟酰胺可以同时使用吗？│   │
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │
│  │  [AI图标]                   │    │ ◀ 思考过程（可折叠）
│  │  ▼ 思考过程（点击折叠）      │    │
│  │  正在分析成分兼容性...       │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │
│  │  系统回答区（流式输出）      │    │ ◀ 回答区
│  │  ...                        │    │
│  │                             │    │
│  │  [1][2]  引用 2 篇资料      │    │ ◀ 引用标注
│  └─────────────────────────────┘    │
│                                     │
│  [👍] [👎] [复制] [重新生成] < 1/1 >│ ◀ 结果操作栏
│                                     │
│  猜你想问：                          │ ◀ 追问胶囊
│  [胶囊1]  [胶囊2]  [胶囊3]          │
│                                     │
├─────────────────────────────────────┤
│  模式：[快速模式 ▼] 语调：[亲和 ▼]  │
├─────────────────────────────────────┤
│  [输入你的问题...          ] [⏹停止] │
└─────────────────────────────────────┘
```

##### 6.4.2.2 用户提问区

| 元素 | 规则 |
|------|------|
| 展示内容 | 用户发送的文本原文 |
| 布局 | 右对齐，带用户头像 |
| 时间戳 | 显示发送时间，格式：HH:mm；跨天显示：MM-DD HH:mm |

##### 6.4.2.3 模型思考过程

| 状态 | 展示规则 |
|------|---------|
| 思考中 | 展示"正在思考..."动态加载动效，内容实时追加 |
| 默认状态 | **展开**（可见） |
| 思考完成 | 自动**收起**（折叠） |
| 用户手动操作 | 支持随时点击展开/折叠，状态持久保留在本次对话中 |
| 快速模式 | **不展示**思考过程（快速模式下无 Reasoning 链路） |
| 专家模式 | **展示**思考过程 |

> **绑定规则**：模式、语调、回答与思考过程相互绑定，每次回答结果对应生成时的参数快照。用户在同一会话中切换模式不影响历史轮次的展示。

##### 6.4.2.4 系统回答区

**排版联动规则：**

| 模式 | 语调 | 排版风格 | 示例 |
|------|------|---------|------|
| 快速模式 | 亲和语调 | 小红书式：Emoji 穿插、重点加粗、短句分段、高可读性 | 「✨ **视黄醇** 和 **烟酰胺** 其实可以一起用哦！...」 |
| 快速模式 | 专业语调 | 简洁专业：无 Emoji，短段落，结论前置 | 「视黄醇与烟酰胺在常规浓度下可配伍使用...」 |
| 专家模式 | 亲和语调 | 结构化 + 亲切措辞：有章节划分，但语言平易 | 「我们来深入看看这两个成分～...」 |
| 专家模式 | 专业语调 | 学术报告式：结构化分段、严谨术语、参考文献 | 「一、成分概述...二、配伍机制...三、参考文献...」 |

**流式输出：**
- 采用 SSE（Server-Sent Events）或 WebSocket 实现流式渲染。
- 文字逐字/逐词流式追加，带打字机动效。
- 渲染 Markdown 格式（加粗、标题、列表、表格）。
- 代码块高亮显示。

**引用资料：**
- 在回答底部展示引用标注：`[1][2]` 脚注或"引用 X 篇资料"文字。
- 点击脚注可展开引用来源详情（来源名称、链接、摘要）。

##### 6.4.2.5 结果操作栏（工具条）

| 操作 | 触发条件 | 行为 | 反馈 |
|------|---------|------|------|
| 👍 点赞 | 回答完成后可操作 | 变为高亮态，记录反馈到服务端 | Toast "反馈成功" |
| 👎 点踩 | 回答完成后可操作 | 弹出反馈弹窗（P-07） | 提交后变为高亮态，Toast "反馈成功" |
| 复制 | 任意时刻 | 一键复制当前版本纯文本结果到剪贴板 | Toast "复制成功" |
| 重新生成 | 回答完成后；最多 5 次 | 基于同一 Prompt 重新调用模型 | 新版本追加，翻页切换 |

**点赞/点踩互斥规则：**
- 点赞后点踩：取消点赞，标记为点踩，弹出反馈弹窗。
- 点踩后点赞：取消点踩，标记为点赞，Toast "反馈成功"。
- 同一状态再次点击：取消当前状态（切换回中性）。

**P-07 点踩反馈弹窗：**

```
┌─────────────────────────────────────┐
│  你觉得这个回答哪里有问题？            │
├─────────────────────────────────────┤
│  问题类别（单选 必选）                │
│  ○ 有害/不安全    ○ 虚假信息         │
│  ○ 没有帮助       ○ 其他            │
├─────────────────────────────────────┤
│  补充说明（必填，1-300字）            │
│  ┌─────────────────────────────┐    │
│  │                             │    │
│  └─────────────────────────────┘    │
│                           0/300     │
├─────────────────────────────────────┤
│  [取消]              [提交反馈]      │
└─────────────────────────────────────┘
```

| 字段 | 规则 |
|------|------|
| 问题类别 | 单选，必选；选项：有害/不安全、虚假信息、没有帮助、其他 |
| 补充说明 | 文本框，必填；最少 1 字符，最多 300 字符；实时字数统计 |
| 提交按钮 | 两项均满足时可点击；提交中显示 Loading |
| 取消 | 关闭弹窗，不保存状态 |

##### 6.4.2.6 重新生成与翻页

| 规则 | 说明 |
|------|------|
| 最大重新生成次数 | 5 次（共 6 个版本：原始 + 5 次重生成） |
| 超过次数 | 隐藏「重新生成」按钮 |
| 版本排序 | 最新生成的排在第一位（Version N → 1 倒序） |
| 翻页控件 | `< 1 / N >` 形式，左右箭头切换；当前版本号高亮显示 |
| 状态绑定 | 每个版本各自独立保留其生成时的模式、语调、思考过程 |

##### 6.4.2.7 猜你想问

| 规则 | 说明 |
|------|------|
| 触发时机 | 系统完整回答完毕后，底部自动追加 |
| 展示数量 | 3 个追问胶囊 |
| 内容来源 | 模型基于上下文对话生成的推荐追问 |
| 点击行为 | 直接将胶囊文案作为新问题发送，进入下一轮对话 |
| 刷新 | 重新生成后，胶囊随新版本重新生成 |

##### 6.4.2.8 底部输入区

| 元素 | 规则 |
|------|------|
| 模式切换 | 下拉选择器，选项：快速模式 / 专家模式 |
| 语调切换 | 下拉选择器，选项：亲和语调 / 专业语调 |
| 字数限制 | 最少 1 字符，最多 1000 字符 |
| 字数不足 | 输入不足 1 字符时，发送按钮置灰 |
| 字数达到上限 | 到达 1000 字符时截断不可继续输入，输入框下方显示红色提示"已达最大字数限制（1000字）" |
| 发送按钮状态 | 正常：「发送」；AI 输出中：「⏹ 停止生成」 |
| 停止生成 | 点击后强制中断当前流式输出，展示已输出内容，状态切换为"对话已中断" |
| 换行 | Shift+Enter 换行，Enter 发送（PC 端）；移动端点击发送按钮发送 |

##### 6.4.2.9 回到最新

| 规则 | 说明 |
|------|------|
| 触发条件 | 页面向上滑动超过一屏距离时，右下角浮现「回到最新 ↓」图标按钮 |
| 点击行为 | 平滑滚动到最新一条消息底部 |
| 消失条件 | 当页面已在底部时，按钮自动隐藏 |

---

### 6.5 历史记录管理（侧边栏）

#### 6.5.1 侧边栏结构

```
┌───────────────────────┐
│  ✕                    │（关闭按钮）
│  + 新建对话            │（常驻按钮）
├───────────────────────┤
│  今天                  │（时间分组）
│  ┌───────────────────┐│
│  │ 视黄醇和烟酰胺可以...││◀ 会话卡片（10字+...）
│  └───────────────────┘│
│  ┌───────────────────┐│
│  │ 防晒霜的PA和SPF有...││
│  └───────────────────┘│
├───────────────────────┤
│  昨天                  │
│  ...                  │
├───────────────────────┤
│  更早                  │
│  ...                  │
└───────────────────────┘
```

#### 6.5.2 新建对话

| 规则 | 说明 |
|------|------|
| 按钮位置 | 侧边栏顶部常驻 |
| 点击行为 | 关闭侧边栏；清空当前对话内容；返回引导页（P-05）空状态 |
| 当前对话保存 | 若当前有未保存的对话，新建时自动保存当前会话到历史记录 |

#### 6.5.3 历史列表

| 规则 | 说明 |
|------|------|
| 排序 | 按最后交互时间倒序（最新在最上） |
| 分组 | 按日期分组：今天 / 昨天 / 最近 7 天 / 更早 |
| 标题生成 | 系统抓取该会话首个问题的首句，截取前 10 个字符，超出用"..."省略 |
| 点击会话 | 加载该会话的历史对话内容，恢复至该会话末尾状态 |
| 空状态 | 无历史记录时展示"暂无历史对话"提示 |

#### 6.5.4 长按/更多交互

长按会话卡片，或点击卡片右侧「···」按钮，弹出操作菜单：

**修改名称：**

| 规则 | 说明 |
|------|------|
| 弹出输入框 | 预填当前名称 |
| 字符限制 | 最多 30 个字符，实时计数 |
| 为空校验 | 不可提交，输入框标红提示"名称不能为空" |
| 提交 | 更新服务端，侧边栏实时刷新名称 |

**删除对话：**

| 规则 | 说明 |
|------|------|
| 二次确认 | 弹窗文案："确定要永久删除该对话吗？删除后不可恢复。" |
| 按钮 | 【取消】【确定删除】（删除按钮红色） |
| 确认后 | 逻辑删除（服务端标记），从列表移除，不可恢复 |

---

### 6.6 设置与偏好

#### 6.6.1 核心角色配置入口

- 路径：**首页 → 更多 → 设置 → 核心角色**
- 在原有设置列表中新增"核心角色"配置项
- 展示当前选中状态（若无则展示"未选择"）

#### 6.6.2 角色配置页（P-10）

| 元素 | 规则 |
|------|------|
| 展示方式 | 二级页面或底部弹出层（由设计决定，保持与 P-04 风格一致） |
| 选项 | 与角色选择弹窗一致，单选 |
| 当前状态 | 高亮显示已选角色 |
| 操作 | 选择后点击「保存」或即时生效（依赖设计方案） |

#### 6.6.3 修改角色后的影响

| 影响项 | 规则 |
|--------|------|
| 生效时机 | 修改后即时生效 |
| 首页推荐 | 刷新后展示新角色对应推荐问题 |
| 新建对话 | 后续新建对话使用新角色的推荐机制与默认话术 |
| 历史对话 | 不受影响，保留原有角色的对话内容 |

---

### 6.7 博美智研后台

#### 6.7.1 功能概述

在会员中心 > 会员管理中，为用户数据新增"核心角色"字段管理能力。

#### 6.7.2 覆盖页面

| 页面 | 改动说明 |
|------|---------|
| 会员列表页 | 新增"核心角色"列，展示当前角色枚举值；未填写显示"-" |
| 新增会员页 | 新增"核心角色"选择字段（非必填下拉框） |
| 编辑会员页 | 新增"核心角色"编辑字段（非必填下拉框） |

#### 6.7.3 字段规格

| 字段名 | 类型 | 是否必填 | 选项 | 默认值 | 未填展示 |
|--------|------|---------|------|--------|---------|
| 核心角色 | 枚举（单选） | 否 | 美妆产品经理 / 配方研发工程师 / 法律合规人员 / 细分领域专家 | 无 | "-" |

#### 6.7.4 搜索与筛选（建议新增）

- 会员列表页支持按"核心角色"筛选用户，方便运营人员进行分角色的运营动作。
- 列表支持按"核心角色"列排序（枚举字母顺序）。

#### 6.7.5 数据权限

- 后台管理员可查看和修改用户的核心角色字段。
- 操作日志记录修改人、修改时间、修改前后的角色值。

---

## 7 异常与边界规则

### 7.1 安全合规

| 场景 | 规则 | 前端展示 |
|------|------|---------|
| 用户输入触发敏感词/违禁词 | 前端本地过滤或后端拦截 | 输入框下方提示："您的问题包含不当内容，请更换问题" |
| 大模型输出中途触发违规内容 | 后端阻断流式输出 | 系统回答区展示兜底文案："暂时无法回答，请更换问题再提问。" |
| 已输出部分内容后触发违规 | 后端截断，已输出内容保留，追加兜底提示 | 已输出内容 + "⚠️ 内容已被拦截，请更换问题再提问。" |

### 7.2 网络与接口

| 场景 | 触发条件 | 前端展示 |
|------|---------|---------|
| API 超时 | >30 秒无响应 | "网络开小差了，请检查网络或点击 [重新生成]" |
| 网络断开 | 网络不可用 | 同上，Banner 提示"当前网络不可用" |
| AI 接口服务异常 | HTTP 5xx | "服务繁忙，请稍后再试" + 展示重新生成按钮 |
| 流式输出中断 | 中途连接断开 | 展示已输出内容，底部提示"回答可能不完整，点击 [重新生成]" |
| 首页推荐接口异常 | 接口返回错误 | 展示兜底推荐问题（本地配置） |

### 7.3 并发限制（Rate Limiting）

| 规则 | 说明 |
|------|------|
| 限制维度 | 同一账号 |
| 限制频率 | 每分钟最高 20 次提问 |
| 超限提示 | Toast "您问得太快了，请稍作休息" |
| 超限行为 | 当分钟内提问超过 20 次后，发送按钮置灰，倒计时显示剩余冷却时间（例："冷却中 47s"） |
| 后台记录 | 超限事件记录日志，供反作弊分析 |

### 7.4 数据边界

| 场景 | 规则 |
|------|------|
| 输入框最大字数 | 1000 字符，到达上限截断，红色提示 |
| 单条回答最大 Token | 由后端 LLM 配置限制，前端不感知，超长答案正常流式展示 |
| 单会话最大轮数 | 建议 ≤ 50 轮（防止上下文过长影响性能）；超出后提示"当前对话轮数已上限，建议新建对话" |
| 历史记录最大条数 | 单用户最多保存 500 条历史会话；超出时删除最旧的会话 |
| 会话名称最大字符 | 30 字符 |
| 反馈补充说明 | 1–300 字符 |

### 7.5 空状态处理

| 场景 | 展示 |
|------|------|
| 历史记录为空 | "暂无历史对话，快去和 AI 聊聊吧～" |
| 推荐问题接口返回空 | 展示本地配置的兜底 3 道题 |
| 猜你想问生成为空 | 不展示猜你想问区域 |
| 引用资料为空 | 不展示引用区域 |

---

## 8 非功能性需求

### 8.1 性能要求

| 指标 | 目标值 | 测量方式 |
|------|--------|---------|
| 首 Token 响应时长（P50） | ≤ 2 秒 | 服务端埋点 |
| 首 Token 响应时长（P90） | ≤ 3 秒 | 服务端埋点 |
| 完整回答输出时长（P90） | ≤ 30 秒 | 服务端埋点 |
| 首页加载时长（P90） | ≤ 2 秒 | Lighthouse / 性能监控 |
| 页面切换流畅度 | 无明显卡顿（FPS ≥ 55） | 前端性能监控 |
| 流式渲染帧率 | 文字追加无抖动 | 视觉验收 |

### 8.2 可用性要求

| 指标 | 目标值 |
|------|--------|
| 智能助手服务可用率 | ≥ 99.5%（月维度） |
| 计划内停机 | 提前 24 小时通知用户，选择低峰时间（凌晨 2:00–4:00） |
| 降级策略 | AI 服务不可用时，展示"智能助手暂时维护中，请稍后再试"，不影响其他功能使用 |

### 8.3 安全性要求

| 要求 | 说明 |
|------|------|
| 传输加密 | 全程 HTTPS/TLS 1.2+ |
| Token 安全 | JWT Token 有效期 7 天，滑动刷新；敏感操作需重新验证 |
| 微信授权 | 遵循微信开放平台 OAuth 2.0 规范 |
| 输入过滤 | 前端 + 后端双重 XSS 过滤，防注入 |
| 敏感词 | 接入平台统一敏感词库，支持动态更新 |
| 用户数据 | 对话内容加密存储，不用于模型训练（需在隐私协议中声明） |
| 日志脱敏 | 日志中的手机号、用户 ID 脱敏处理 |

### 8.4 兼容性要求

| 平台 | 要求 |
|------|------|
| iOS | iOS 15+ |
| Android | Android 9.0+，内核 Chrome 88+ |
| 微信内置浏览器 | 微信 8.0+ |
| Web 端（桌面浏览器） | Chrome 90+、Safari 15+、Firefox 90+、Edge 90+ |
| 屏幕尺寸 | 手机：360dp–430dp；平板：768dp+（响应式布局） |
| 网络环境 | 支持 4G/5G/WiFi；弱网下（2G/3G）展示加载态，超时后友好提示 |

### 8.5 无障碍（Accessibility）

| 要求 | 说明 |
|------|------|
| 对比度 | 文字与背景对比度 ≥ 4.5:1（WCAG AA 标准） |
| 字体可缩放 | 支持系统字体大小设置（最大放大至 200%） |
| 屏幕阅读器 | 关键交互元素添加 `aria-label`，图标按钮有语义描述 |
| 焦点管理 | 弹窗打开时聚焦首个可交互元素，关闭后焦点回到触发元素 |

---

## 9 数据模型

### 9.1 用户扩展字段（User Extension）

| 字段名 | 类型 | 说明 | 可选值 |
|--------|------|------|--------|
| `core_role` | Enum / String | 用户核心角色 | `PRODUCT_MANAGER` / `FORMULA_ENGINEER` / `LEGAL_COMPLIANCE` / `DOMAIN_EXPERT` / `null` |
| `role_selected_at` | DateTime | 角色首次设置时间 | - |
| `role_onboarding_shown` | Boolean | 角色选择弹窗是否已展示过 | true / false |
| `preferred_mode` | Enum | 偏好模式 | `QUICK` / `EXPERT`，默认 `QUICK` |
| `preferred_tone` | Enum | 偏好语调 | `FRIENDLY` / `PROFESSIONAL`，默认 `FRIENDLY` |
| `wechat_open_id` | String | 微信 OpenID（微信登录绑定） | - |
| `phone_bound_at` | DateTime | 手机号绑定时间 | - |

### 9.2 会话（Conversation）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | UUID | 会话唯一标识 |
| `user_id` | UUID | 所属用户 |
| `title` | String | 会话标题（首句前 10 字） |
| `created_at` | DateTime | 创建时间 |
| `updated_at` | DateTime | 最后更新时间 |
| `is_deleted` | Boolean | 逻辑删除标记 |
| `message_count` | Integer | 会话消息总轮数 |

### 9.3 消息（Message）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | UUID | 消息唯一标识 |
| `conversation_id` | UUID | 所属会话 |
| `role` | Enum | `USER` / `ASSISTANT` |
| `content` | Text | 消息内容（Markdown） |
| `mode` | Enum | 生成时的模式：`QUICK` / `EXPERT` |
| `tone` | Enum | 生成时的语调：`FRIENDLY` / `PROFESSIONAL` |
| `thinking_process` | Text | 思考过程内容（Reasoning 链路） |
| `citations` | JSON Array | 引用来源列表 |
| `version_index` | Integer | 同一 Prompt 下的版本号（从 1 开始） |
| `is_current_version` | Boolean | 是否为当前展示版本 |
| `feedback_type` | Enum | 用户反馈：`LIKE` / `DISLIKE` / `null` |
| `created_at` | DateTime | 消息创建时间 |
| `finish_reason` | Enum | `COMPLETE` / `STOPPED` / `ERROR` / `FILTERED` |

### 9.4 反馈（Feedback）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | UUID | 反馈唯一标识 |
| `message_id` | UUID | 关联的消息 |
| `user_id` | UUID | 提交用户 |
| `feedback_type` | Enum | `DISLIKE` |
| `category` | Enum | `HARMFUL` / `MISINFORMATION` / `NOT_HELPFUL` / `OTHER` |
| `description` | String(300) | 补充说明 |
| `created_at` | DateTime | 提交时间 |

### 9.5 推荐问题（RecommendedQuestion）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | UUID | 问题唯一标识 |
| `content` | String | 问题文案 |
| `target_role` | Enum / null | 适用角色（null 表示全局通用） |
| `type` | Enum | `HOMEPAGE` / `GUIDE_PAGE` |
| `sort_order` | Integer | 排序权重 |
| `is_active` | Boolean | 是否启用 |
| `created_at` | DateTime | 创建时间 |

---

## 10 接口需求说明

> 本章列出前后端协作所需的关键接口，具体字段与错误码由后端技术设计文档（TDD）详细定义。

### 10.1 登录相关接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 微信授权登录 | POST | `/api/v1/auth/wechat` | 携带微信 code，返回 Token 或跳转手机绑定标识 |
| 手机号绑定 | POST | `/api/v1/auth/bind-phone` | 验证短信码，完成手机绑定并登录 |
| 获取短信验证码 | POST | `/api/v1/auth/sms-code` | 发送验证码到指定手机号 |
| 退出登录 | POST | `/api/v1/auth/logout` | 使当前 Token 失效 |

### 10.2 用户相关接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取用户信息 | GET | `/api/v1/user/profile` | 返回当前用户信息含 core_role |
| 更新核心角色 | PATCH | `/api/v1/user/profile` | 更新 core_role 字段 |

### 10.3 推荐问题接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取推荐问题 | GET | `/api/v1/recommend/questions` | 参数：`role`（可选），返回 3 个推荐问题；若无结果返回全局兜底 |

### 10.4 对话相关接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 新建会话 | POST | `/api/v1/conversations` | 返回新会话 ID |
| 获取会话详情 | GET | `/api/v1/conversations/{id}` | 返回会话信息及消息列表 |
| 获取历史会话列表 | GET | `/api/v1/conversations` | 分页返回，按 updated_at 倒序 |
| 删除会话 | DELETE | `/api/v1/conversations/{id}` | 逻辑删除 |
| 更新会话名称 | PATCH | `/api/v1/conversations/{id}` | 更新 title 字段 |
| 发送消息（流式） | POST | `/api/v1/conversations/{id}/messages/stream` | SSE 流式返回，携带 mode、tone 参数 |
| 重新生成 | POST | `/api/v1/conversations/{id}/messages/{msg_id}/regenerate` | 基于同一 Prompt 重新生成 |
| 停止生成 | POST | `/api/v1/conversations/{id}/messages/{msg_id}/stop` | 强制终止流式输出 |
| 提交反馈 | POST | `/api/v1/messages/{id}/feedback` | 提交点赞/点踩及详细反馈 |
| 获取猜你想问 | GET | `/api/v1/conversations/{id}/suggestions` | 基于上下文返回 3 个追问建议 |

### 10.5 流式消息格式（SSE）

```
event: delta
data: {"type": "thinking", "content": "正在分析成分..."}

event: delta
data: {"type": "answer", "content": "视黄醇和烟酰胺"}

event: delta
data: {"type": "answer", "content": "可以同时使用。"}

event: done
data: {"citations": [...], "suggestions": [...], "finish_reason": "COMPLETE"}

event: error
data: {"code": "CONTENT_FILTERED", "message": "暂时无法回答，请更换问题再提问。"}
```

### 10.6 通用错误码

| HTTP 状态码 | 错误码 | 说明 |
|------------|--------|------|
| 400 | `INVALID_PARAMS` | 参数错误 |
| 401 | `UNAUTHORIZED` | 未登录或 Token 失效 |
| 403 | `FORBIDDEN` | 无权限 |
| 429 | `RATE_LIMITED` | 触发频控（20次/分钟） |
| 500 | `INTERNAL_ERROR` | 服务器内部错误 |
| 503 | `SERVICE_UNAVAILABLE` | AI 服务不可用 |
| - | `CONTENT_FILTERED` | 内容被安全策略拦截 |

---

## 11 埋点与数据分析

### 11.1 埋点规范

所有埋点事件统一格式：

```json
{
  "event": "事件名",
  "user_id": "用户ID",
  "session_id": "会话ID（可选）",
  "timestamp": "ISO8601时间戳",
  "properties": { ... }
}
```

### 11.2 关键埋点事件

| 事件名 | 触发时机 | 关键属性 |
|--------|---------|---------|
| `ai_home_view` | 进入智能助手首页 | `is_login`, `role` |
| `ai_hot_topic_click` | 点击热门话题 | `question_content`, `role`, `is_fallback` |
| `ai_role_modal_show` | 角色选择弹窗展示 | `trigger_scene` |
| `ai_role_selected` | 点击确认并进入 | `role`, `trigger_scene` |
| `ai_role_skipped` | 点击跳过 | `trigger_scene` |
| `ai_message_send` | 用户发送消息 | `mode`, `tone`, `char_count`, `is_suggestion_click` |
| `ai_message_receive_start` | 收到首个 Token | `latency_ms` |
| `ai_message_receive_done` | 回答完整输出 | `total_duration_ms`, `mode`, `tone`, `has_citations` |
| `ai_message_stopped` | 用户点击停止生成 | `elapsed_ms`, `output_length` |
| `ai_feedback_like` | 点赞 | `message_id` |
| `ai_feedback_dislike_open` | 打开点踩弹窗 | `message_id` |
| `ai_feedback_dislike_submit` | 提交点踩反馈 | `category`, `description_length` |
| `ai_copy_click` | 点击复制 | `message_id` |
| `ai_regenerate_click` | 点击重新生成 | `message_id`, `current_version` |
| `ai_suggestion_click` | 点击猜你想问胶囊 | `suggestion_content` |
| `ai_new_conversation` | 点击新建对话 | - |
| `ai_history_view` | 打开历史记录侧边栏 | - |
| `ai_history_item_click` | 点击历史会话 | `conversation_id` |
| `ai_history_delete` | 确认删除会话 | `conversation_id` |
| `ai_thinking_expand` | 展开思考过程 | `message_id` |
| `ai_thinking_collapse` | 折叠思考过程 | `message_id` |
| `ai_mode_switch` | 切换模式 | `from`, `to` |
| `ai_tone_switch` | 切换语调 | `from`, `to` |
| `ai_error_shown` | 展示错误提示 | `error_type`, `error_code` |
| `wechat_login_click` | 点击微信登录 | - |
| `wechat_login_success` | 微信授权登录成功 | `is_new_user`, `need_bind_phone` |
| `wechat_login_fail` | 微信授权失败 | `reason` |

---

## 12 测试要求

### 12.1 功能测试用例（关键路径）

#### 登录模块

| 用例编号 | 测试场景 | 预期结果 |
|---------|---------|---------|
| TC-L-01 | 微信授权登录 - 已绑定手机号 | 直接登录成功，跳转目标页 |
| TC-L-02 | 微信授权登录 - 未绑定手机号 | 跳转手机绑定页，完成绑定后登录 |
| TC-L-03 | 微信授权失败/取消 | 返回登录页，Toast"授权失败，请重试" |
| TC-L-04 | 手机号格式错误 | 获取验证码按钮置灰，提示格式错误 |
| TC-L-05 | 验证码过期 | 提示"验证码已过期，请重新获取" |
| TC-L-06 | 退出登录 | Token 清空，返回首页非登录态 |

#### 角色选择

| 用例编号 | 测试场景 | 预期结果 |
|---------|---------|---------|
| TC-R-01 | 首次注册后弹出角色选择弹窗 | 弹窗展示且不可点击背景关闭 |
| TC-R-02 | 选择角色并确认 | 角色保存成功，跳转对应目标页 |
| TC-R-03 | 跳过角色选择 | 角色记为未选择，下次不再自动弹出 |
| TC-R-04 | 未选角色点击确认 | 按钮置灰或提示"请先选择一个角色" |
| TC-R-05 | 已登录未设角色，点击热门话题 | 弹出角色选择弹窗 |

#### 对话工作区

| 用例编号 | 测试场景 | 预期结果 |
|---------|---------|---------|
| TC-C-01 | 输入问题并发送 | 流式输出正常，思考过程展示（专家模式） |
| TC-C-02 | 快速模式下发送问题 | 不展示思考过程，小红书式排版 |
| TC-C-03 | 专家+专业语调发送问题 | 展示思考过程，学术报告排版 |
| TC-C-04 | 思考完成后自动收起 | 思考框自动折叠 |
| TC-C-05 | 点击停止生成 | 流式输出中断，展示已输出内容 |
| TC-C-06 | 点赞 | 高亮，Toast"反馈成功" |
| TC-C-07 | 点踩并提交反馈 | 弹窗展示，提交后高亮 |
| TC-C-08 | 点赞后点踩 | 点赞状态取消，激活点踩弹窗 |
| TC-C-09 | 复制回答 | 剪贴板有内容，Toast"复制成功" |
| TC-C-10 | 重新生成 5 次 | 第 6 次按钮隐藏，翻页显示 1/6 |
| TC-C-11 | 输入 1001 字符 | 截断在 1000 字符，红色提示 |
| TC-C-12 | 猜你想问点击 | 自动发送为新问题 |
| TC-C-13 | 内容触发敏感词 | 前端拦截，提示换题 |
| TC-C-14 | 回到最新按钮 | 上滑后出现，点击滚回底部 |

#### 历史记录

| 用例编号 | 测试场景 | 预期结果 |
|---------|---------|---------|
| TC-H-01 | 新建对话 | 清空对话，回到引导页 |
| TC-H-02 | 历史列表按时间倒序 | 最新会话在最上方 |
| TC-H-03 | 修改会话名称 - 正常 | 保存成功，列表实时更新 |
| TC-H-04 | 修改会话名称 - 为空 | 提示不可为空，不可提交 |
| TC-H-05 | 删除会话二次确认 | 确认后从列表移除，不可恢复 |

### 12.2 异常场景测试

| 用例编号 | 测试场景 | 预期结果 |
|---------|---------|---------|
| TC-E-01 | AI 接口超时 >30s | 展示超时错误提示和重新生成按钮 |
| TC-E-02 | 每分钟发送 21 次 | 第 21 次拦截，Toast 提示 |
| TC-E-03 | 断网后发送消息 | Banner 提示网络不可用 |
| TC-E-04 | 流式输出中途断连 | 展示已输出内容，提示不完整 |

### 12.3 性能测试要求

| 场景 | 并发量 | 目标 |
|------|--------|------|
| 首页加载 | 200 并发 | P90 ≤ 2s |
| 发送消息（流式） | 100 并发 | 首 Token P90 ≤ 3s |
| 历史列表加载 | 200 并发 | P90 ≤ 1s |

### 12.4 兼容性测试要求

- iOS 15、16、17 各主流机型验收
- Android 9、10、12、14 各主流机型验收
- 微信内置浏览器（iOS + Android）验收
- PC Chrome / Safari 验收

---

## 13 发布与灰度计划

### 13.1 发布阶段

| 阶段 | 覆盖范围 | 目标 | 退出条件 |
|------|---------|------|---------|
| 内测（Alpha） | 内部员工（~50人） | 发现主流程 Bug | 主流程无阻断 Bug |
| 小范围灰度（Beta） | 5% 用户 | 验证性能与用户体验 | 错误率 <1%，P90 响应达标 |
| 扩大灰度 | 30% 用户 | 验证推荐与 AI 质量 | 负向反馈率 <8% |
| 全量发布 | 100% 用户 | 正式上线 | 各指标稳定 |

### 13.2 回滚策略

- 出现以下情况立即回滚：
  - AI 服务可用率跌破 95%
  - 错误率超过 5%
  - 出现数据安全事故
- 回滚方案：关闭智能助手入口，不影响其他功能使用；后端降级关闭 AI 能力。

### 13.3 监控报警

| 监控项 | 报警阈值 | 通知方式 |
|--------|---------|---------|
| 接口错误率 | >1% | 钉钉/企微 + 短信 |
| P90 首 Token 时长 | >5s | 钉钉/企微 |
| 并发限制触发率 | >5% | 钉钉/企微（观察） |
| 敏感词拦截率异常波动 | 较基线变化 >30% | 钉钉/企微 |

---

## 14 待确认问题（Open Questions）

| 编号 | 问题描述 | 负责人 | 期望确认时间 | 状态 |
|------|---------|--------|------------|------|
| OQ-01 | 微信登录回调域名需注册到微信开放平台，需确认域名配置方案 | 技术负责人 | TBD | 待确认 |
| OQ-02 | 流式输出协议选型：SSE vs WebSocket？SSE 更简单，但 WebSocket 支持双向通信，需根据是否需要中断指令决定 | 技术负责人 | TBD | 待确认 |
| OQ-03 | 推荐问题的运营配置后台是否本期实现，还是先以代码配置代替？ | 产品负责人 | TBD | 待确认 |
| OQ-04 | 历史记录是否支持搜索功能？本期文档未提及，但可能是用户高频需求 | 产品负责人 | TBD | 待确认 |
| OQ-05 | 多端同步：用户在手机端的历史记录是否在 PC Web 端可见？ | 技术负责人 | TBD | 待确认 |
| OQ-06 | 思考过程（Reasoning）是否计入 Token 费用？需要了解采用的模型计费策略 | 算法负责人 | TBD | 待确认 |
| OQ-07 | 「猜你想问」是模型实时生成还是规则引擎？对响应时长有影响 | 算法负责人 | TBD | 待确认 |
| OQ-08 | 用户对话数据的隐私合规处理方案，是否需要用户明确授权？ | 法务/合规 | TBD | 待确认 |
| OQ-09 | 单会话最大轮数限制（当前建议 50 轮），是否与业务方对齐？ | 产品负责人 | TBD | 待确认 |
| OQ-10 | 「回到最新」按钮的触发阈值（向上滑多少距离出现）需设计明确定义 | 设计负责人 | TBD | 待确认 |

---

## 15 术语表

| 术语 | 释义 |
|------|------|
| LLM | Large Language Model，大语言模型 |
| SSE | Server-Sent Events，服务器推送事件，实现单向流式通信 |
| 流式输出 | AI 模型边生成边输出，实现打字机效果，提升用户体验 |
| Reasoning / 思考链路 | 大模型在给出最终答案前的内部推理过程，部分模型（如 o1）支持输出 |
| Prompt | 发送给 AI 模型的指令文本，包含用户问题和系统设定 |
| Token | AI 模型处理文本的基本单位，一个 Token 约等于 0.75 个英文单词或 0.5 个中文字 |
| 兜底策略 | 当主流程异常时，使用预先定义的默认内容或行为保障基本体验 |
| 角色（Core Role） | 用户在平台内设置的职业身份，用于定向推荐和个性化回答 |
| 快速模式 | 优先速度，给出简洁直接的回答，不展示 Reasoning |
| 专家模式 | 优先深度，给出结构化、专业的深度回答，展示 Reasoning |
| 亲和语调 | 使用 Emoji、口语化表达，增强亲切感 |
| 专业语调 | 使用学术化、结构化语言，增强专业性 |
| Toast | 页面底部或顶部短暂显示的轻量级通知提示，2–3 秒后自动消失 |
| 点赞/点踩 | 用户对 AI 回答质量的正向/负向反馈 |
| 猜你想问 | AI 根据上下文主动推荐的追问方向，以胶囊按钮形式展示 |
| 灰度发布 | 逐步将新功能开放给小比例用户，验证稳定性后再全量发布 |
| Rate Limiting | 频控，限制单用户在单位时间内的请求次数，防止滥用 |
| DAU | Daily Active Users，日活跃用户数 |
| P90 | 第 90 百分位值，即 90% 的请求满足该条件 |
| KPI | Key Performance Indicator，关键绩效指标 |
| PRD | Product Requirements Document，产品需求文档 |
| TDD | Technical Design Document，技术设计文档 |

---

*文档结束*

> **审阅记录**：本文档需经产品、技术、设计、测试、运营各负责人确认签字后方可作为开发基准。请各团队负责人在收到文档后 2 个工作日内完成审阅并提出修改意见。
